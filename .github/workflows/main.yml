name: Build and Deploy
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install and Build
        run: |
          npm install
          ng build  --prod --output-path public --base-href="https://linkit-tech.gitlab.io/tools/signature-generator/"

